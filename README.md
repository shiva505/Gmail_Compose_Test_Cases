echo "# Gmail Compose Test Cases" > README.md
echo "This repository contains QA test cases for testing the compose function in Gmail." >> README.md
git add README.md
git commit -m "Add README file"
git push
